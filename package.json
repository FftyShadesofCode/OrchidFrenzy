{
  "scripts": {
    "start": "npm-run-all --parallel backend frontend",
    "frontend": "cd frontend && npm run start",
    "backend": "cd backend && npm run start",
    "instfront": "cd frontend && npm i",
    "instback": "cd backend && npm i",
    "inst": "npm-run-all --parallel instback instfront"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
